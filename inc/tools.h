#ifndef TOOLS_H
# define TOOLS_H

#include <stdlib.h>
#include <string.h>
#include <stdbool.h>

char	*addChar(char *str, char c);
bool	isOnStr(char *str, char c);
size_t	getFreq(char *str, char c);

#endif
